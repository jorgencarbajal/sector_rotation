# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# API token
.env

# databases (BAMLH0A0HYM2.csv is committed - it is the only source for the full HY OAS history)
data/*.db
data/*.db-journal

# backtest output, overwritten every run
results/

# stray temp file from an interrupted CSV export
data/*.tmp
